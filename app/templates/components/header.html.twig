<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        {# Nav-brand-button #}
        <a href="#" class="navbar-brand" >Navbar</a>
        {# Nav-hamburger-toggle #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDarkDropdown" aria-controls="navbarNavDarkDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        {# Nav-items #}
        <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">

            <ul class="navbar-nav">
                <li class="nav-item d-flex flex-column flex-md-row justify-content-evenly gap-4 my-2 mx-auto">
                    <a href="{{ url('listing_create') }}" class="btn btn-primary">{{ 'action.listing_create'|trans }}</a>
                    <a href="{{ url('listing_index') }}" class="btn btn-info">{{ 'action.go_to_listings'|trans }}</a>
                    <a href="{{ url('category_index') }}" class="btn btn-warning">{{ 'action.go_to_categories'|trans }}</a>
                    <a href="{{ url('listing_index_activate') }}" class="btn btn-success">{{ 'action.go_to_activation'|trans }}</a>
                </li>

            </ul>
        </div>


    </div>
</nav>